const dataFields = {
	code        : '[AbbrevCode]',
	description : '[AbbrevDescription]',
};

const dateTimeFields = {};

const timeFields = {};

const dbModel = {
	tableName      : '[Ref Medicine Administration Codes]',
	dataFields     : dataFields,
	dateTimeFields : dateTimeFields,
	timeFields     : timeFields,
};

module.exports.dbModel = dbModel;
